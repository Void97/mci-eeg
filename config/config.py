import torch

# PARAMS = {
#     'batch size': 32,
#     'epochs': 80,
#     'learning rate': 0.00001,
#     'beta1': 0.5,
#     'beta2': 0.999,
#     'L2': 0.0001,
#     'device': 'cuda' if torch.cuda.is_available() else 'cpu'
# }

PARAMS = {
    'batch size': 32,
    'epochs': 80,
    'learning rate': 0.00001,
    'beta1': 0.5,
    'beta2': 0.999,
    'L2': 0.0001,
    'device': 'cuda' if torch.cuda.is_available() else 'cpu'
} 
